"""This program lets you download packages to the correct pip library."""


# import subprocess
# import sys

# This program is optional and makes sure that DialogueKit (use python 3.9)
# or other libraries are downloaded at the correct place.

# def install(package):
#    subprocess.check_call([sys.executable, "-m", "pip", "install", package])

# def pip_list():
#    subprocess.check_call([sys.executable, "-m", "pip", "list"])

# def run_moviebot():
#    subprocess.check_call([sys.executable, "-m", "moviebot.run"])

# RUN ONLY OF THESE BELOW AT A TIME!

# install("dialoguekit")
# pip_list()
# run_moviebot()
